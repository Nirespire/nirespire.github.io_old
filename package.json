{
  "name": "sanjaynair.me",
  "version": "1.0.0",
  "description": "Personal website. Built using the nuxt.js SSG.",
  "dependencies": {
    "nuxt": "^1.4.0"
  },
  "scripts": {
    "dev": "nuxt",
    "build": "nuxt build",
    "start": "nuxt start",
    "test": "echo \"Error: no test specified\" && exit 0",
    "deploy": "push-dir --dir=.nuxt/dist --branch=master --cleanup"
  },
  "author": "Sanjay Nair",
  "license": "MIT",
  "devDependencies": {
    "push-dir": "^0.4.1"
  }
}
